/target
Cargo.lock.bak
*.swp
*.swo
.DS_Store

# WASM build output (generated by `wasm-pack` during the Pages workflow;
# also reproduced locally with the same command — never committed).
/web/pkg/
/examples/wasm-demo/pkg/

graphify-out
